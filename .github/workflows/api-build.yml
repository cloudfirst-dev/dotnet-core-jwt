name: .NET Core

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: vrutkovs/action-s2i@master
      with:
        path: /home/runner/work/dotnet-core-jwt/dotnet-core-jwt/api
        base: registry.centos.org/dotnet/dotnet-21-centos7
        image: quay.io/cloudfirst/dotnet-core-jwt-api:latest
